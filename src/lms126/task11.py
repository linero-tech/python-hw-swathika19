from to_do import TODO


def task11():
    return """
    Enter the input value to be converted into a variable tempinput
    check IF the variable tempinput contains a character c
    then we have to convert into Fareinheit 
    take only numbers from tempinput into tempinputn
    tempoutput=(tempinputn x 1.8) + 32 
    print tempoutput+F
    else
    if the tempinput contains a character F
    then we have to convert to celsius
    Take only numbers from tempinput into tempinputn
    tempoutput= (tempinputn - 32) x .5556 
    Print tempoutput+C
    """
